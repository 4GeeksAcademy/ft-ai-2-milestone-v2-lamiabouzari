# Secrets & Environment

- Never commit `.env`, API keys, payment credentials, or real customer data.
- Use `.env.example` with placeholder values when env vars are needed.
- Do not hardcode secrets in source, Storybook, or memory-bank files.
