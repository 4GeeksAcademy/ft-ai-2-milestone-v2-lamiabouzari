# Verification Before Marking Work Done

1. App builds and runs (`npm run dev` / `npm run build`) with no type errors.
2. Changed views render and match spec sections affected by the change.
3. No edits were made to `uis/website_v1/` unless that was the stated goal.
4. Shared types compile and are imported correctly from `@repo/shared-types`.
5. Memory bank updated if an architectural decision was made.
